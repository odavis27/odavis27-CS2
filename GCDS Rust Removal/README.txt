How to use:

Run the file.
You will be prompted to insert the dimensions (length, width, height) of the package as 
well as the two zip codes it from travel from at to.
The inputs should be inserted in the following order:
    - length
    - width
    - height
    - starting zip code
    - target zip code

after inserting information of the package, the program will print the cost of the package